'use client'

import styled from "styled-components"

export const Button = styled.button`
   color:black;
   background: #F1EFE3;
   margin:.6rem 0;
   padding:1rem .5rem;
   border-radius: 30px;

`